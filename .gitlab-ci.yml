image: node:15.12-alpine3.13

stages:
  - test
  - deploy

test:
  stage: test
  script:
  - yarn
  - yarn build
  except:
    - master

pages:
  stage: deploy
  script:
  - yarn
  - yarn build
  - mv ./build ./public
  artifacts:
    paths:
    - public
  only:
    - master
